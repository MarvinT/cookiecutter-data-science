#!/bin/bash

git init
git add *
git commit -m 'initial cookie cutter commit'
# conda env create -f env.yaml
# conda activate {{ cookiecutter.env_name }}
# pre-commit install
# pre-commit run --all-files